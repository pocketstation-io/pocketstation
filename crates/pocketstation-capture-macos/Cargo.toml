[package]
name = "pocketstation-capture-macos"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "macOS CoreAudio process tap and HAL plugin loopback capture for PocketStation"

[dependencies]
pocketstation-capture = { path = "../pocketstation-capture", version = "0.1.0" }
pocketstation-frame = { path = "../pocketstation-frame", version = "0.1.0" }

[build-dependencies]
cc = "1"
