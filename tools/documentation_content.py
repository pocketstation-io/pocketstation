"""Reviewed, page-specific human documentation content.

This module deliberately contains authored responsibility, task, and symptom
models. Evidence inventories remain generated from ``.doc-intel``; these
records prevent those inventories from being mistaken for people-first docs.
"""

from __future__ import annotations


CONCEPTS = {
    "DOC-006": {
        "what": "A `Session` is the declaration-time owner of capture sources, external sources, operators, endpoints, routes, and recording intent. You assemble that model before PocketStation compiles or opens runtime resources.",
        "why": "Keeping declaration separate from execution lets PocketStation reject cross-Session handles, missing destinations, incompatible ports, and unsupported topology before an active runtime owns devices or workers.",
        "relationships": ["`SessionSpec` is the frozen declaration produced from the mutable draft.", "`CompiledSession` resolves the declaration into a typed plan; `PreparedSession` owns prepared resources.", "`RunningSession` owns active execution and produces structured stop and recording outcomes."],
        "invariants": ["A declaration handle belongs to exactly one Session.", "Freezing the draft ends mutation; later route changes are rejected.", "A source or derived stream must reach a destination before the declaration can become a valid plan."],
        "use": ["[Run the Rust quickstart](/docs/getting-started/rust-quickstart.md)", "[Prepare resources before start](/docs/how-to/prepare-session.md)", "[Stop and inspect failures](/docs/how-to/stop-session.md)"],
    },
    "DOC-007": {
        "what": "Source selection turns an application, process, microphone, or system query into a capture identity. A query states what you want; resolution records which concrete source matched at that observation point.",
        "why": "Process identifiers, application membership, devices, and permissions can change. A distinct resolution step keeps those changes visible instead of treating a display name or process ID as permanent identity.",
        "relationships": ["`ApplicationSelector` and capture queries express selection intent.", "`CaptureSelection` records the resolved candidate and identity strength.", "Source generation and permission epoch report changes after selection."],
        "invariants": ["An empty or malformed stable key is rejected.", "Process-scoped selection retains the exact process instance rather than only a numeric PID.", "Resolution evidence does not prove the source can be opened; preparation returns that result."],
        "use": ["[Select a process-scoped source](/docs/how-to/select-process-source.md)", "[Capture a desktop application](/docs/how-to/capture-application.md)", "[Diagnose missing application audio](/docs/troubleshooting/no-application-audio.md)"],
    },
    "DOC-008": {
        "what": "Application capture opens audio associated with a selected desktop application through the backend compiled for the current target.",
        "why": "Application-scoped capture keeps one application's audio distinct from microphone and system-loopback sources, so routes, observations, and recordings retain their source identity.",
        "relationships": ["Source selection identifies the application before preparation.", "`ApplicationPolicyObservation` projects allowed, denied, unobservable, or not-applicable policy state without conflating it with the source-open result.", "The platform backend owns native opening and callback delivery.", "Frame lineage carries application source identity into routes and recording stems."],
        "invariants": ["Implementation availability and physical qualification are separate claims.", "A successful selection or allowed policy observation does not imply permission or source opening succeeded.", "The host application owns selection UI and permission prompts."],
        "use": ["[Capture a desktop application](/docs/how-to/capture-application.md)", "[Capture application and microphone stems](/docs/how-to/capture-app-and-mic.md)", "[No application audio arrives](/docs/troubleshooting/no-application-audio.md)"],
    },
    "DOC-009": {
        "what": "Microphone capture selects the default or an identified input device and opens it through the target's native capture backend.",
        "why": "Microphone audio has independent permission, device identity, timing, and failure state. Modeling it as its own source prevents application audio and input-device behavior from being conflated.",
        "relationships": ["Permission observation can inform UI before preparation but is not the open result.", "The selected microphone becomes a source with its own stem and lineage.", "Polled audio and recording routes consume that stem independently."],
        "invariants": ["`NotObservable` means no reliable preflight answer, not denial.", "The source-open outcome is authoritative.", "The default device may change; retain the identity returned for the active source."],
        "use": ["[Capture the default microphone](/docs/how-to/capture-microphone.md)", "[Observe permission without prompting](/docs/how-to/observe-permission.md)", "[No microphone audio arrives](/docs/troubleshooting/no-microphone-audio.md)"],
    },
    "DOC-010": {
        "what": "System capture represents a platform loopback source where the selected backend implements one. It captures a broader system mix than application-scoped capture.",
        "why": "Loopback capture has different platform support and permission behavior from application or microphone capture, so it needs an explicit source mode and qualification boundary.",
        "relationships": ["A system query expresses loopback intent.", "The platform module selects the target implementation.", "The resulting frames use the same routing and lineage contracts as other audio sources."],
        "invariants": ["A source-query variant does not guarantee that the current target can open it.", "Target-specific source files prove implementation presence only.", "Unsupported opening remains a typed result rather than a silent fallback to another source."],
        "use": ["[Capture system audio](/docs/how-to/capture-system-audio.md)", "[Review platform compatibility](/docs/platform/compatibility.md)", "[Diagnose permission state](/docs/troubleshooting/permission-state.md)"],
    },
    "DOC-011": {
        "what": "Permission and source lifecycle records separate authorization observation from source generation, disappearance, reappearance, and open outcomes.",
        "why": "Desktop authorization is not one universal process-wide Boolean. Keeping observation, prompts, and open results distinct prevents advisory state from being promoted to a guarantee.",
        "relationships": ["The host application owns prompts and user-facing permission flow.", "Capture preparation or opening returns the authoritative typed result.", "Permission epochs and source generations identify changes that affect later frames."],
        "invariants": ["Observation does not prompt.", "`NotObservable` is neither granted nor denied.", "Frames retain the generation and permission evidence in effect when they were produced."],
        "use": ["[Observe permission without prompting](/docs/how-to/observe-permission.md)", "[Permission state is denied or unobservable](/docs/troubleshooting/permission-state.md)", "[A capture source disappears](/docs/troubleshooting/source-loss.md)"],
    },
    "DOC-012": {
        "what": "Frame lineage is immutable metadata that identifies the Session, source, stem, stream, route, clock domain, sequence, generation, and derivation associated with audio.",
        "why": "PCM alone cannot explain which source produced it, which route delivered it, or whether a discontinuity occurred. Lineage makes those answers available to consumers and diagnostics.",
        "relationships": ["`AudioFrame` combines sample storage, sample specification, and lineage.", "Clock mapping correlates lineage timestamps with the Session timeline.", "Signals use corresponding lineage so operator output remains attributable."],
        "invariants": ["A frame's identity is not rewritten when it crosses a route.", "Derived output records its upstream derivation rather than pretending to be an original source.", "Sequence, generation, and discontinuity changes remain observable."],
        "use": ["[Map source time](/docs/how-to/map-source-time.md)", "[Preserve source identity](/docs/best-practices/source-identity.md)", "[Investigate timestamp divergence](/docs/troubleshooting/timing.md)"],
    },
    "DOC-013": {
        "what": "A clock domain identifies the authority and origin that produced a timestamp. `describe_clock_domain` distinguishes unspecified, PocketStation process-monotonic, and provider-defined domains; `TimelineMapping` correlates source time with the Session timeline while drift components describe bounded correction.",
        "why": "Capture devices and processing components do not necessarily advance from the same origin or at the same rate. Explicit domains prevent unrelated timestamps from being compared as if they shared one clock.",
        "relationships": ["Frame lineage carries the source clock-domain ID and timestamp.", "`SessionTimelineOrigin` and monotonic nanosecond observations provide the common Session correlation origin.", "Route enqueue, route receive, endpoint enqueue, and application poll timestamps expose distinct delivery boundaries.", "Drift observations and correction operate on mappings without replacing lineage."],
        "invariants": ["Clock-domain ID zero declares no clock authority; ID one is PocketStation's process-monotonic nanosecond clock; other IDs preserve provider-defined origin rather than inventing an epoch.", "A wall-clock timestamp is not substituted for a clock-domain mapping.", "Delivery-boundary timestamps are observations, not a universal end-to-end latency guarantee.", "Unrepresentable timestamp arithmetic returns an absent or typed result.", "Discontinuities advance evidence rather than silently smoothing history."],
        "use": ["[Map source time into the Session timeline](/docs/how-to/map-source-time.md)", "[Timestamps diverge or discontinuities appear](/docs/troubleshooting/timing.md)"],
    },
    "DOC-014": {
        "what": "Audio buffer pools own a finite set of reusable sample buffers. Frames borrow or share that storage according to the route's ownership and copy contract.",
        "why": "Realtime audio cannot rely on unbounded allocation. A finite pool makes storage exhaustion visible and keeps ownership transitions explicit.",
        "relationships": ["`AudioBufferPool` acquires reusable storage.", "`AudioFrame` freezes initialized storage for downstream consumers.", "Copy policy and fan-out determine whether a route shares or copies frame storage."],
        "invariants": ["A writer cannot submit more samples than the acquired capacity.", "Only initialized samples become part of a frozen frame.", "Pool exhaustion is an observable result, not permission to allocate without bound."],
        "use": ["[Inject external PCM](/docs/how-to/inject-external-pcm.md)", "[Keep realtime callbacks bounded](/docs/best-practices/realtime-boundaries.md)", "[External PCM input is saturated](/docs/troubleshooting/external-pcm.md)"],
    },
    "DOC-015": {
        "what": "Session compilation validates a frozen declaration, resolves named bindings and media compatibility, and lowers the result into an executable plan.",
        "why": "Compilation separates structural mistakes from resource-opening failures. Developers can therefore distinguish an invalid graph from a device, endpoint, or worker that could not be prepared.",
        "relationships": ["`SessionSpec` is the compiler input.", "Graph contracts provide port, media, partition, and edge requirements.", "`SessionCompileDiagnostic` projects the compile stage, stable error code, affected component IDs, and explanatory fields without reducing the failure to display text.", "`CompiledSession` is passed to preparation, which owns external resource work."],
        "invariants": ["Unknown, duplicate, foreign, or incompatible declarations fail before runtime execution.", "A required named input must be bound.", "Multiple stems connected to one operator retain separate source-aware input bindings.", "Compilation does not prove that a native source or endpoint can start."],
        "use": ["[Prepare resources before start](/docs/how-to/prepare-session.md)", "[Connect named operator ports](/docs/how-to/connect-operator-ports.md)", "[Session fails before start](/docs/troubleshooting/session-start.md)"],
    },
    "DOC-016": {
        "what": "Preparation converts a compiled plan into owned source, endpoint, operator, bridge, and mapping resources without yet transferring the Session to steady-state running ownership.",
        "why": "Resource acquisition can fail after structural validation. A distinct phase keeps those failures attributable and gives rollback a defined set of already-prepared components.",
        "relationships": ["Compilation produces the plan and declaration identities.", "Preparation creates runtime mappings back to those identities.", "Start consumes prepared resources or returns a staged failure with rollback evidence."],
        "invariants": ["Prepared resources preserve their declaration IDs.", "A primary preparation failure is not replaced by a rollback failure.", "A prepared Session is not yet evidence that callbacks or workers reached running state."],
        "use": ["[Prepare resources before start](/docs/how-to/prepare-session.md)", "[Build, prepare, and start lifecycle](/docs/lifecycle/build-prepare-start.md)", "[Session fails before start](/docs/troubleshooting/session-start.md)"],
    },
    "DOC-017": {
        "what": "`RunningSession` owns the active Session engine, capture workers, routes, endpoints, observation handles, and finalization responsibilities until stop completes.",
        "why": "Active resources need one visible lifetime owner. Retaining the running handle prevents stop, recording finalization, and component failures from being reduced to implicit drop behavior.",
        "relationships": ["Start transfers a prepared Session into `RunningSession`.", "Observation handles expose running metrics and events without taking ownership.", "Stop returns Session, component, recording, and trace outcomes."],
        "invariants": ["Keep the running owner alive while consuming frames or observations.", "Stop and finalization are structured outcomes, not a Boolean success flag.", "Drop behavior does not create an undocumented retry or drain guarantee."],
        "use": ["[Stop a Session and inspect failures](/docs/how-to/stop-session.md)", "[Running ownership](/docs/lifecycle/running.md)", "[Session stop reports failures](/docs/troubleshooting/session-stop.md)"],
    },
    "DOC-018": {
        "what": "The polled-audio endpoint places routed frames in a fixed-capacity queue and returns bounded batch leases through a receipt owned by application code.",
        "why": "Polling gives non-realtime application code a pull interface without turning the audio callback into an unbounded producer or copying every frame into unlimited storage.",
        "relationships": ["A Session route sends one source or stream to the endpoint.", "`try_poll` returns immediately, while `wait_poll` waits only until its caller-supplied finite timeout.", "Each leased frame exposes route enqueue, route receive, endpoint enqueue, and poll observation timestamps.", "The endpoint driver owns queue and lease observations.", "A batch lease bounds how many frames remain outstanding at once."],
        "invariants": ["Queue, batch, and outstanding-lease capacities are non-zero and bounded.", "An empty queue, a finite wait timeout, and exhausted lease capacity are distinct outcomes.", "The wait does not create an additional audio queue.", "Release a lease promptly so capacity returns to the receipt."],
        "use": ["[Poll audio without unbounded buffering](/docs/how-to/poll-audio.md)", "[Frames or signals are dropped](/docs/troubleshooting/drops-and-saturation.md)"],
    },
    "DOC-019": {
        "what": "Multistem recording writes independent labeled source stems under one Session recording root and reports per-stem finalization after stop.",
        "why": "Keeping application and microphone output separate preserves source identity and makes partial file failure visible instead of hiding it behind one mixed recording result.",
        "relationships": ["Recording is declared on source stems before start.", "Frame lineage and timeline mapping align recorded output.", "Stable constants identify the default recording group, manifest file name, and manifest schema version for SDK projections.", "The recording outcome is available after endpoint finalization during Session stop."],
        "invariants": ["Each recorded stem has its own identity and outcome.", "A running Session does not imply that every output file or recording manifest finalized.", "The additive manifest constants do not change the externally constructible `RecordingOutcome` shape.", "Inspect completed and failed stems before treating the recording as successful."],
        "use": ["[Record independent stems](/docs/how-to/record-stems.md)", "[Inspect recording outcomes](/docs/how-to/inspect-recording-outcome.md)", "[A recording is incomplete](/docs/troubleshooting/recording-incomplete.md)"],
    },
    "DOC-020": {
        "what": "Graph contracts describe port direction, signal and media compatibility, multiplicity, execution partition, safety, copy, loss, delivery, and observability before a route is compiled.",
        "why": "A processing graph crosses ownership and execution boundaries. Explicit contracts let the compiler reject incompatible or unsafe connections rather than discovering them after workers start.",
        "relationships": ["Node and operator manifests own named ports.", "Edge contracts control finite delivery between compatible ports.", "The runtime plan preserves the validated topology and policies."],
        "invariants": ["Input and output direction must agree.", "Signal and media specifications must be compatible.", "Realtime partitions cannot claim operations that violate their declared safety contract."],
        "use": ["[Choose route capacity and loss policy](/docs/how-to/configure-route-policy.md)", "[Connect named operator ports](/docs/how-to/connect-operator-ports.md)", "[Graph API reference](/docs/reference/graph.md)"],
    },
    "DOC-021": {
        "what": "An asynchronous operator is a registered factory and runtime node that consumes and emits named typed signals outside the realtime callback lane.",
        "why": "Model calls, network I/O, and asynchronous processing need allocation and awaiting that are not valid in the realtime audio path. The operator boundary moves that work onto an explicit async partition.",
        "relationships": ["`AsyncOperatorManifest` declares ports and execution requirements.", "A factory prepares a node for one compiled operator instance.", "Generated PCM returns to the audio lane only through the audio-reentry bridge."],
        "invariants": ["Ports used by the declaration must exist in the registered manifest.", "Output signal identity and media must match the declared port.", "Cancellation and final output follow the operator runtime contract."],
        "use": ["[Implement an asynchronous operator](/docs/how-to/implement-operator.md)", "[Connect named operator ports](/docs/how-to/connect-operator-ports.md)", "[Return generated PCM](/docs/how-to/return-generated-audio.md)"],
    },
    "DOC-022": {
        "what": "Signals carry text, event, metrics, control, binary, custom-schema, or audio-adjacent payloads with a declared specification, timing, and lineage. Streams are declaration handles used to compose their routes.",
        "why": "Typed envelopes keep payload interpretation and provenance explicit across asynchronous operators instead of treating every message as an unstructured byte buffer.",
        "relationships": ["`SignalSpec` identifies the payload class and optional schema.", "`SignalEnvelope` owns payload, timing, and lineage.", "Typed stream handles connect compatible operator ports in a Session declaration."],
        "invariants": ["A payload must match the receiving port's signal specification.", "Lineage remains attached across a derived signal chain.", "Payload and queue limits are enforced before fan-out."],
        "use": ["[Implement an asynchronous operator](/docs/how-to/implement-operator.md)", "[Connect named operator ports](/docs/how-to/connect-operator-ports.md)", "[Signals API reference](/docs/reference/graph.md)"],
    },
    "DOC-023": {
        "what": "Audio reentry is the bounded crossing that accepts generated PCM from asynchronous work and returns it to a typed audio route.",
        "why": "Async operators cannot write directly into the realtime lane without an ownership, format, capacity, and cancellation boundary. The bridge makes those constraints observable.",
        "relationships": ["An operator declares generated-audio output.", "`GeneratedAudioBridgeSpec` fixes Session, stem, stream, source, clock, sample format, frame size, and pool capacity.", "Bridge observations appear in Session metrics and terminal diagnostics."],
        "invariants": ["PCM must match the declared sample specification and frame size.", "Pool and route capacity remain finite.", "Closed, cancelled, saturated, and invalid-format outcomes remain distinct."],
        "use": ["[Return generated PCM through a bridge](/docs/how-to/return-generated-audio.md)", "[Frames or signals are dropped](/docs/troubleshooting/drops-and-saturation.md)"],
    },
    "DOC-024": {
        "what": "The endpoint lifecycle prepares a destination driver, waits for its start gate, delivers declared inputs, requests cancellation or stop, joins its worker, and returns finalization observations.",
        "why": "Destinations can fail at different stages. A staged contract keeps preparation, start, delivery, cancellation, join, and finalization failures attributable.",
        "relationships": ["An endpoint manifest declares its inputs and preparation group.", "`EndpointDriverFactory` creates a prepared driver.", "Session stop collects endpoint failures without discarding other component outcomes."],
        "invariants": ["Delivered media must match the endpoint input contract.", "The driver reports readiness through its start gate.", "Finalization is part of terminal outcome handling, not an assumed side effect."],
        "use": ["[Implement an endpoint driver](/docs/how-to/implement-endpoint.md)", "[Stop, drain, and finalization](/docs/lifecycle/stop-drain-finalize.md)", "[Session stop reports component failures](/docs/troubleshooting/session-stop.md)"],
    },
    "DOC-025": {
        "what": "A connector manifest describes a provider endpoint's identity, node and port contracts, configuration schema, secret fields, delivery requirements, and readiness behavior without adding provider protocol details to Core.",
        "why": "Provider integrations need a stable typed boundary while credentials and network protocols remain connector-owned. The manifest is the declaration and validation authority for that boundary.",
        "relationships": ["Connector configuration resolves typed and secret values against the manifest.", "A connector driver runs behind the endpoint lifecycle.", "Conformance checks portable contract behavior separately from live provider qualification."],
        "invariants": ["Connector and node identities must be valid and unique.", "Secret values use `ConnectorSecret` and redact their debug representation.", "Delivery and readiness state do not imply a retry budget that the API does not declare."],
        "use": ["[Author a connector](/docs/guides/connectors.md)", "[Configure connector secrets](/docs/how-to/configure-connector-secrets.md)", "[Connector reference](/docs/reference/connectors.md)"],
    },
    "DOC-026": {
        "what": "A connector worker owns delivery, acknowledgement, readiness observation, cancellation, drain or abort, and the connector's terminal outcome.",
        "why": "Provider delivery can fail independently from Session capture and other endpoints. Worker ownership keeps that failure stage and its observations local to the connector.",
        "relationships": ["The manifest and resolved configuration are preparation inputs.", "The endpoint lifecycle starts and stops the connector driver.", "`ConnectorRetryability` classifies failure policy while observations count retry attempts; no finite retry budget API is declared."],
        "invariants": ["Delivery outcomes are finite and typed.", "Readiness, delivery, retryability, and shutdown stages remain distinguishable.", "A connector failure does not erase other component and recording outcomes."],
        "use": ["[Author a connector](/docs/guides/connectors.md)", "[Honor connector retryability](/docs/best-practices/connector-retries.md)", "[A connector is not ready](/docs/troubleshooting/connector-readiness.md)"],
    },
    "DOC-027": {
        "what": "A native extension library is trusted executable code that exports the versioned PocketStation entry point and contributes source, operator, or endpoint registrations.",
        "why": "Dynamic loading crosses file-system, ABI, callback, and executable-lifetime boundaries. An explicit library owner keeps those checks and registrations transactional.",
        "relationships": ["The C ABI defines descriptor and callback layouts.", "The loader canonicalizes the path and validates the entry point and ABI version.", "A load receipt retains the library while imported callbacks remain reachable."],
        "invariants": ["The input path is absolute, canonical, and a regular file.", "Registrations are rolled back if import fails.", "The host—not PocketStation—decides whether the library is trusted to execute."],
        "use": ["[Build and load a native extension](/docs/guides/extensions.md)", "[Load extensions from trusted paths](/docs/best-practices/native-extension-trust.md)", "[A native extension does not load](/docs/troubleshooting/native-extension-load.md)"],
    },
    "DOC-028": {
        "what": "The C ABI exposes versioned structures, opaque handles, status values, callbacks, and release functions for Session and extension operations.",
        "why": "Managed and native SDKs need a stable binary boundary whose ownership and error behavior do not depend on Rust layout or panic propagation.",
        "relationships": ["`pocketstation.h` is the consumer-facing declaration authority.", "Rust ABI wrappers translate typed outcomes into stable C status structures.", "Conformance executables verify layout and cross-language behavior under recorded fixtures."],
        "invariants": ["Callers set and check the declared ABI version and structure size.", "Every created handle is released through its matching function.", "Rust panics are contained and translated at the ABI boundary."],
        "use": ["[Operate a Session through C](/docs/how-to/use-c-session-api.md)", "[C ABI reference](/docs/reference/c-abi.md)", "[Run protocol checks](/docs/how-to/run-protocol-checks.md)"],
    },
    "DOC-029": {
        "what": "A sidecar is a managed child process that exchanges bounded messages with a host through an explicit startup, readiness, cancellation, drain, abort, and terminal-state protocol.",
        "why": "Some integrations must run outside the Rust process. The sidecar contract keeps process lifetime, message limits, and deadlines observable instead of treating the child as an unbounded subprocess call.",
        "relationships": ["`SidecarProcessSpec` declares the command and protocol limits.", "`SidecarHost` owns the child and lifecycle deadlines.", "A connector can adapt the sidecar boundary through its driver contract."],
        "invariants": ["Messages remain within declared kinds and byte limits.", "Drain and abort are different shutdown modes.", "Process isolation does not imply authentication or sandboxing."],
        "use": ["[Host a managed-process sidecar](/docs/how-to/host-sidecar.md)", "[A sidecar misses a deadline](/docs/troubleshooting/sidecar-deadline.md)", "[Security boundaries](/docs/security/boundaries.md)"],
    },
    "DOC-030": {
        "what": "Observations are stable metrics, events, and snapshots keyed by Session and component identity. They expose queue, drop, latency, readiness, source, operator, endpoint, connector, sidecar, and terminal state.",
        "why": "Bounded routes and independent components need attributable diagnostics. Aggregate success alone cannot identify which route saturated or which component failed to finalize.",
        "relationships": ["Observation handles read live counters without taking runtime ownership.", "`SessionComponentId` identifies source, endpoint, operator, sidecar, and runtime observations without string parsing.", "Application policy, frame delivery, source faults, discontinuities, and recording state each retain a typed observation boundary.", "Session events report lifecycle changes.", "Traces persist selected lifecycle evidence for later structural validation."],
        "invariants": ["Counters describe the observation boundary that produced them.", "Missing metrics are not converted into zero unless the type contract says so.", "Observations support diagnosis but do not create performance guarantees."],
        "use": ["[Instrument a Session](/docs/how-to/instrument-session.md)", "[Size routes from observations](/docs/best-practices/route-sizing.md)", "[Observations reference](/docs/reference/observations.md)"],
    },
    "DOC-031": {
        "what": "A Session trace is a persisted sequence of versioned lifecycle records that can be checked for identity, ordering, and terminal consistency.",
        "why": "Runtime logs may be unstructured or incomplete. A trace preserves a machine-readable diagnostic artifact tied to stable component and Session identities.",
        "relationships": ["The running Session records versioned `SessionTraceRecord` values for lifecycle changes and component failures.", "Each record carries a sequence index, monotonic observation time, Session ID, and typed record kind.", "`SessionTraceTerminal` preserves terminal state plus source, endpoint, rollback, and finalization failure counts.", "Stop finalizes the trace separately from recording output.", "Trace validation returns the failing record and structural reason."],
        "invariants": ["Record order, Session identity, component identity, and terminal counts must satisfy the trace format.", "A terminal trace includes the required terminal record.", "Dropped recorder entries remain observable in the recorder outcome.", "Validation does not rewrite malformed evidence to make it pass."],
        "use": ["[Instrument a Session](/docs/how-to/instrument-session.md)", "[Session trace validation fails](/docs/troubleshooting/session-trace.md)", "[Lifecycle evidence reference](/docs/reference/lifecycle-evidence.md)"],
    },
    "DOC-032": {
        "what": "External PCM input is a source extension that gives application code a finite buffer, validates its sample shape, and submits the initialized frame into Session routing.",
        "why": "Externally produced audio needs the same capacity, ownership, lineage, and cancellation rules as native capture without pretending to be a platform device.",
        "relationships": ["`AudioInputConfig` declares sample format and bounded capacity.", "A writer acquires and fills an `AudioInputBuffer`.", "The submitted source uses ordinary Session routes and observations."],
        "invariants": ["Writes stay within acquired capacity and match the declared frame shape.", "Acquisition exhaustion, write rejection, closure, and cancellation are distinct.", "Submitted frames preserve Session and source lineage."],
        "use": ["[Inject external PCM](/docs/how-to/inject-external-pcm.md)", "[External PCM input is saturated](/docs/troubleshooting/external-pcm.md)"],
    },
    "DOC-033": {
        "what": "PocketStation's Opus API owns stateful encoder and decoder instances configured by sample rate, channels, frame duration, application mode, and bitrate.",
        "why": "Opus frames have constrained rates and sizes, and codec state spans calls. Typed profiles keep those constraints close to construction and conversion.",
        "relationships": ["`OpusConfig` creates encoder state.", "Audio frames provide PCM input and packet output retains the selected profile.", "The decoder must accept the corresponding packet and output shape."],
        "invariants": ["Only supported Opus sample rates, channel layouts, and frame durations are accepted.", "Input PCM length must match the selected frame configuration.", "Codec errors remain typed and do not imply that retrying the same state is safe."],
        "use": ["[Encode and decode Opus](/docs/how-to/opus-roundtrip.md)", "[Opus conversion fails](/docs/troubleshooting/opus.md)", "[Codec reference](/docs/reference/codec.md)"],
    },
    "DOC-034": {
        "what": "PocketStation exposes typed Rust failures and stable cross-boundary error or status codes. Error stages and terminal outcomes retain which operation and component failed.",
        "why": "Text alone cannot support reliable branching or cross-language compatibility. Typed variants and stable codes let callers distinguish declaration, preparation, runtime, stop, and finalization failures.",
        "relationships": ["Rust APIs return owning error types.", "Error-code mappings preserve selected classes at ABI and persisted boundaries.", "Terminal outcomes can contain multiple component failures after partial progress."],
        "invariants": ["An error name does not establish retry safety.", "The primary failure is preserved when rollback also fails.", "Unknown retry, recovery, or fatality remains explicitly undocumented rather than guessed."],
        "use": ["[Error code index](/docs/reference/error-codes.md)", "[Stop a Session and inspect failures](/docs/how-to/stop-session.md)", "[Error references](/docs/errors/session.md)"],
    },
    "DOC-035": {
        "what": "Conformance is executable evidence that an ABI, protocol, connector, or cross-language boundary matches a versioned fixture under recorded conditions. Qualification is evidence from a real provider, operating system, or device environment.",
        "why": "A passing build or synthetic vector can prove contract compatibility without proving physical capture or live-provider behavior. Separate labels prevent one evidence scope from silently becoming another.",
        "relationships": ["Repository fixtures exercise deterministic Session and extension behavior.", "The canonical connector vector corpus lives at `scripts/fixtures/connector-v1-vectors.json`; validation copies it to the versioned sibling path consumed by the portable-semantics test.", "CI and release checks record commands and targets but do not replace physical qualification."],
        "invariants": ["Missing fixtures are prerequisite failures, not passing evidence.", "Conformance results name their source revision and fixture revision.", "Qualification claims remain limited to the environment actually tested."],
        "use": ["[Run protocol checks](/docs/how-to/run-protocol-checks.md)", "[Test connector conformance](/docs/how-to/test-connector-conformance.md)", "[Conformance fixtures are missing](/docs/troubleshooting/conformance-fixtures.md)"],
    },
    "DOC-036": {
        "what": "The release evidence boundary is the set of package metadata, workflow jobs, protocol checks, tests, examples, and artifacts recorded for a published revision.",
        "why": "A release note is a declaration, while executable checks establish narrower facts. Keeping the evidence layers separate prevents publication metadata from becoming an unsupported product guarantee.",
        "relationships": ["`Cargo.toml` declares the crate version and feature surface.", "CI and publish workflows select validation commands.", "Release notes summarize changes and link to the validated revision."],
        "invariants": ["Every release claim remains scoped to its recorded command and environment.", "An implemented platform path is not automatically physically qualified.", "External or private prerequisites remain visible in the completion report."],
        "use": ["[Release and version information](/RELEASE_NOTES.md)", "[Keep qualification claims scoped](/docs/best-practices/evidence-boundaries.md)", "[Run protocol checks](/docs/how-to/run-protocol-checks.md)"],
    },
    "DOC-037": {
        "what": "The Whisper example is a separate Cargo package that captures PocketStation stems and crosses into an external transcription process while recording process evidence.",
        "why": "Transcription behavior depends on an executable and environment outside the core crate. The example makes that integration boundary visible instead of claiming the SDK itself performs transcription.",
        "relationships": ["PocketStation capture and polling supply audio stems.", "The example owns conversion and child-process invocation.", "A process-evidence artifact records what external work actually ran."],
        "invariants": ["Example compilation proves API compatibility, not process availability.", "A transcript without matching process evidence is not accepted as verified execution.", "The external process contract does not expand Core's guarantees."],
        "use": ["[Run the transcription example](/docs/how-to/run-transcription.md)", "[Transcription process evidence is missing](/docs/troubleshooting/transcription-evidence.md)"],
    },
    "DOC-056": {
        "what": "Cargo features select whether the crate exposes contracts only, the default native-capture implementation, conformance fixtures, or repository-internal testing surfaces.",
        "why": "Consumers that only need type contracts should not be forced to compile native capture dependencies, while validation builds need deliberate access to feature-gated fixtures.",
        "relationships": ["The default feature enables native capture.", "`conformance-fixtures` exposes deterministic cross-language fixtures.", "`internal-testing` exposes repository validation surfaces and is not a runtime product recommendation."],
        "invariants": ["Feature selection happens at compile time and follows Cargo feature unification.", "Changing features requires rebuilding the crate.", "All feature-gated public declarations remain part of the documentation inventory."],
        "use": ["[Choose crate features](/docs/how-to/choose-features.md)", "[Install PocketStation](/docs/getting-started/installation.md)", "[A native-capture build fails](/docs/troubleshooting/native-build.md)"],
    },
}


GUIDES = {
    "GUIDE-001": ("A target with application capture support, host-owned capture permission, and an application selector that can resolve the intended process.", "Frames from the selected application arrive on its polled route, and stop returns without a source or route failure.", "Separate selector resolution, permission, source opening, and route delivery; each can fail independently.", ["/docs/concepts/application-capture.md", "/docs/reference/capture.md"]),
    "GUIDE-002": ("An available input device and a host application capable of owning the platform permission prompt.", "Microphone frames arrive with a distinct source and stem identity, and the source-open outcome is successful.", "Treat permission observation as advisory and source opening as authoritative.", ["/docs/concepts/microphone-capture.md", "/docs/platform/permissions.md"]),
    "GUIDE-003": ("Both sources can be selected and opened, and you have chosen independent destinations or recording labels.", "Both routes deliver frames, retain different stem identities, and report their own recording outcomes.", "A healthy source does not hide failure or saturation on the other route.", ["/docs/concepts/session.md", "/docs/concepts/multistem-recording.md"]),
    "GUIDE-004": ("A discovery result that exposes process-instance identity rather than only a display name.", "Resolution returns the intended exact process instance and later generation changes remain observable.", "Reject zero process IDs, empty stable keys, and non-application identities instead of weakening the selector.", ["/docs/concepts/source-selection.md", "/docs/reference/capture.md"]),
    "GUIDE-005": ("A host UI that can decide whether and when to present a permission prompt.", "The UI distinguishes granted, denied, and unobservable preflight state, then uses source opening as the final result.", "Do not translate `NotObservable` into either grant or denial.", ["/docs/concepts/permissions-and-source-lifecycle.md", "/docs/platform/permissions.md"]),
    "GUIDE-006": ("A declared source or stream and a finite `PolledAudioEndpointConfig` appropriate for non-realtime polling.", "`try_poll_audio` returns a bounded lease; observed frame counts advance and the lease is released after consumption.", "Distinguish an empty queue from exhausted outstanding-lease capacity and route drops.", ["/docs/concepts/polled-audio.md", "/docs/reference/endpoints.md"]),
    "GUIDE-007": ("One declared source and two independently configured destinations.", "The compiled plan contains distinct routes and each destination exposes its own delivery observations.", "Fan-out does not combine saturation: one constrained branch must remain attributable to that branch.", ["/docs/concepts/realtime-routing.md", "/docs/reference/graph.md"]),
    "GUIDE-008": ("The producer and consumer execution partitions, acceptable loss behavior, and observations you can measure.", "The declaration compiles, route depth stays within capacity, and saturation or drops match the selected policy.", "Capacity is a workload decision; change it only after observing the constrained route.", ["/docs/concepts/graph-contracts.md", "/docs/best-practices/route-sizing.md"]),
    "GUIDE-009": ("A writable recording root and stable labels for every source stem you intend to preserve.", "Stop returns a recording outcome whose completed stems match the declared labels and whose failed-stem list is empty.", "File finalization happens during stop; do not declare success from captured-frame counts alone.", ["/docs/concepts/multistem-recording.md", "/docs/reference/recording.md"]),
    "GUIDE-010": ("A stopped Session with its structured `SessionStopOutcome` still available.", "Overall state, completed stems, failed stems, and per-stem errors have all been inspected before the outcome is discarded.", "A partial recording can coexist with useful completed stems; preserve both sides of the outcome.", ["/docs/concepts/multistem-recording.md", "/docs/errors/recording.md"]),
    "GUIDE-011": ("A unique operator and node identity, named typed ports, and an execution partition appropriate for the work.", "The factory registers, the Session compiles, and the runtime receives and emits the declared signal types.", "Preparation, processing, timeout, final output, and cancellation failures are separate stages.", ["/docs/concepts/async-operators.md", "/docs/reference/graph.md"]),
    "GUIDE-012": ("A registered operator manifest and typed declaration handles from the same Session.", "Compilation binds each named handle to the intended operator instance and compatible port.", "Unknown, duplicate, foreign, or incompatible ports fail before runtime preparation.", ["/docs/concepts/graph-contracts.md", "/docs/errors/graph-and-signals.md"]),
    "GUIDE-013": ("An async operator output declared as generated audio and a target sample specification for the audio lane.", "PCM writes are accepted by the bridge and Session audio-reentry observations advance without format or capacity failure.", "Never write generated PCM directly into the realtime lane or silently change its lineage.", ["/docs/concepts/audio-reentry.md", "/docs/reference/observations.md"]),
    "GUIDE-014": ("An endpoint manifest with declared inputs and a factory that can prepare resources without starting delivery early.", "The start gate reports readiness, declared inputs are consumed, and finalization returns observations or a staged failure.", "Preserve the exact endpoint failure stage and retryability classification.", ["/docs/concepts/endpoints.md", "/docs/reference/endpoints.md"]),
    "GUIDE-015": ("A stable connector identity, port and configuration schema, secret ownership plan, and finite delivery outcomes.", "The connector registers, validates configuration, reaches readiness, consumes its declared item, and finalizes under conformance.", "Conformance is not live-provider qualification, and the Core API does not declare a retry budget.", ["/docs/concepts/connectors.md", "/docs/reference/connectors.md"]),
    "GUIDE-016": ("A manifest field for every accepted value and a decision about which fields contain secret material.", "Configuration resolution returns typed values; diagnostics do not reveal the initialized secret representation.", "Redacted debug output does not prove that every upstream or downstream copy has been erased.", ["/docs/concepts/connectors.md", "/docs/security/boundaries.md"]),
    "GUIDE-017": ("The repository-owned connector vector corpus and permission to materialize its versioned sibling path.", "Local contract tests and portable vector assertions pass, with fixture revision and execution scope recorded.", "A missing or drifted materialized copy is a prerequisite failure, never passing connector evidence.", ["/docs/concepts/conformance.md", "/docs/troubleshooting/conformance-fixtures.md"]),
    "GUIDE-018": ("A dynamic library for the current target, a compatible ABI descriptor, and a host trust decision for its canonical path.", "The loader returns a receipt, all registrations appear together, and the library remains owned while callbacks are reachable.", "Treat path validation, library loading, entry point, ABI, descriptor, and registration as distinct failure stages.", ["/docs/concepts/native-extensions.md", "/docs/reference/native-extensions.md"]),
    "GUIDE-019": ("The repository header, a compatible ABI version, and correct ownership for every opaque handle and callback context.", "Every call returns an accepted `PksSessionStatus`, stop completes, and each handle is released by its matching function.", "Do not let a Rust panic, borrowed pointer, or library context escape its declared ABI lifetime.", ["/docs/concepts/c-abi-ownership.md", "/docs/reference/c-abi.md"]),
    "GUIDE-020": ("A bounded `SidecarProcessSpec`, declared message kinds, and explicit startup and shutdown deadlines.", "The child reaches running state, exchanges only accepted messages, and ends in the requested drained or aborted terminal state.", "A managed process boundary does not itself provide sandboxing or authentication.", ["/docs/concepts/sidecars.md", "/docs/reference/sidecar-protocol.md"]),
    "GUIDE-021": ("Observation handles acquired before the interval you need to diagnose and a writable path if you record a trace.", "Metrics resolve by stable component ID, the trace validates, and stop contributes its terminal record.", "A missing metric is not automatically zero; keep identity and collection timing with the snapshot.", ["/docs/concepts/observability.md", "/docs/concepts/session-traces.md"]),
    "GUIDE-022": ("The `RunningSession` owner and any observation, recording, or trace handles whose terminal state you need.", "The stop outcome, component failures, recording result, and trace finalization have all been retained and classified.", "Request stop once; do not discard partial outcomes because the top-level state is unsuccessful.", ["/docs/concepts/session-lifecycle.md", "/docs/lifecycle/terminal-outcomes.md"]),
    "GUIDE-023": ("An `AudioInputConfig` matching the producer's rate, channels, frame shape, and finite buffer capacity.", "The writer acquires, fills, and submits a buffer whose frames arrive on the declared Session route.", "Acquisition exhaustion, invalid write shape, closure, and cancellation require different responses.", ["/docs/concepts/external-pcm.md", "/docs/reference/capture.md"]),
    "GUIDE-024": ("An Opus profile supported by the crate and PCM frames matching its sample rate, channels, and duration.", "The encoder produces a packet and the matching decoder returns the expected frame shape under the repository round-trip test.", "Keep encoder and decoder state aligned; a codec error does not establish safe replay of the same state.", ["/docs/concepts/opus-codec.md", "/docs/reference/codec.md"]),
    "GUIDE-025": ("The repository toolchain plus every public, sibling, or private fixture named by the selected check.", "Each command exits successfully and its target, feature set, fixture revision, and evidence scope are recorded.", "Separate a missing prerequisite, compilation error, assertion failure, and unqualified physical path.", ["/docs/concepts/conformance.md", "/docs/reference/test-evidence.md"]),
    "GUIDE-026": ("The nested example's Rust and external-process prerequisites, capture access, and a location for process evidence.", "The example package builds, the child process runs, and the evidence artifact matches this source revision.", "Compilation alone does not prove an executable was found or a transcription request completed.", ["/docs/concepts/transcription-integration.md", "/docs/troubleshooting/transcription-evidence.md"]),
    "GUIDE-027": ("The consumer role: native capture application, contracts-only library, conformance harness, or repository test.", "`cargo check` succeeds with the selected feature set and only the intended gated modules are visible.", "Cargo unifies features across the dependency graph, so inspect the resolved build rather than one dependency line in isolation.", ["/docs/concepts/cargo-features.md", "/docs/reference/configuration.md"]),
    "GUIDE-028": ("A target backend that implements system loopback and host permission needed by that platform path.", "The system query resolves, source preparation succeeds, and frames reach the bounded consumer with system-source lineage.", "Never fall back silently from unsupported system capture to application or microphone capture.", ["/docs/concepts/system-capture.md", "/docs/platform/compatibility.md"]),
    "GUIDE-029": ("A stable source clock-domain ID, source timestamp, Session timestamp, and discontinuity policy.", "Mapped timestamps remain representable and drift observations stay within the correction behavior you selected.", "Preserve original lineage even when a controller applies a bounded correction.", ["/docs/concepts/timing-and-clocks.md", "/docs/reference/timing.md"]),
    "GUIDE-030": ("A frozen Session declaration that has already passed structural compilation.", "Every resource mapping is retained, start reaches running state, or a staged failure reports primary and rollback evidence.", "Preparation can perform I/O and resource acquisition; do not treat compilation success as start success.", ["/docs/concepts/runtime-preparation.md", "/docs/lifecycle/build-prepare-start.md"]),
}


TROUBLESHOOTING = {
    "TRBL-001": ("`Session::start` never returns a running owner.", ["The declaration cannot compile because a required route, binding, or compatible port is absent.", "A source, operator, bridge, endpoint, or trace resource fails during preparation.", "Start is cancelled or a prepared component fails its start gate; rollback can add further failures."], "Compare the returned stage with declaration, compile, prepare, and start error types. Then inspect component identity and any rollback-failure count.", "Correct the exact declaration or resource precondition at the failing stage. Preserve rollback failures alongside the primary error.", "Retry only from a newly valid declaration or newly prepared resource state. No frames should be assumed delivered before a running owner is returned.", ["/docs/lifecycle/build-prepare-start.md", "/docs/errors/session.md"]),
    "TRBL-002": ("The Session is running, but the application route yields no frames.", ["The selector resolved no candidate or a different process instance.", "Permission or source opening did not succeed for application capture.", "The source disappeared, changed generation, or emits no frames.", "The intended route is absent, saturated, or polled through the wrong receipt."], "Check resolution identity, source-open outcome, source lifecycle events, per-route observations, and the receipt's delivered-frame count in that order.", "Re-resolve the intended application, correct permission or routing, and recreate only the component whose typed state is invalid.", "Opening or routing failures do not establish safe automatic retry. Frames already delivered on other stems remain independent and may be partial.", ["/docs/concepts/application-capture.md", "/docs/reference/capture.md"]),
    "TRBL-003": ("The Session is running, but the microphone route yields no frames.", ["The host has not granted input permission or the platform cannot observe it before open.", "The default input changed or the selected device is unavailable.", "Microphone preparation or opening failed.", "The microphone route is saturated or the wrong receipt is being polled."], "Compare permission observation with the source-open result, then verify selected device identity and microphone route observations.", "Let the host own any permission prompt, select an available input, and rebuild the affected source route.", "Do not loop on `NotObservable`; retry opening only after a meaningful permission or device change. Other stems may still contain valid frames.", ["/docs/concepts/microphone-capture.md", "/docs/platform/permissions.md"]),
    "TRBL-004": ("Permission preflight reports denied or cannot produce a reliable state.", ["The platform reports a real denial.", "The target exposes no non-prompting observation and returns `NotObservable`.", "Authorization changed after a previous observation."], "Treat denied and unobservable as different values. Compare the permission epoch and use the next source-open outcome as authority.", "For denial, guide the developer through the host's platform UI. For unobservable state, continue only through an explicit user action and handle the open result.", "A denied state is not safe to hammer with retries; an unobservable state is not success. No source frames exist until opening succeeds.", ["/docs/concepts/permissions-and-source-lifecycle.md", "/docs/reference/capture.md"]),
    "TRBL-005": ("A previously selected capture source disappears or returns with a new identity generation.", ["The application exited or its exact process instance changed.", "The input device was removed or the default device changed.", "Permission or platform lifecycle state changed."], "Inspect the source event kind, stable source ID, prior and current generation, and permission epoch. Correlate frames by lineage before and after the event.", "Stop depending routes or resolve and prepare a new source generation. Do not relabel earlier frames as if they came from the replacement.", "A new generation requires a new source state; replay safety is not implied. Previously delivered frames remain valid only with their original lineage.", ["/docs/concepts/permissions-and-source-lifecycle.md", "/docs/best-practices/source-identity.md"]),
    "TRBL-006": ("Route observations report drops, saturation, full queues, or rejected signals.", ["A consumer is slower than its producer at the configured capacity.", "A polled-audio lease remains outstanding and consumes receipt capacity.", "A signal payload exceeds its branch limit.", "The selected loss or delivery policy rejects work at saturation."], "Compare capacity, current depth, peak depth, drop counters, lease counters, and branch identity. Identify one constrained route before changing global settings.", "Release leases, fix the slow consumer, reduce work, or deliberately choose a different capacity and edge policy.", "A dropped item cannot be recovered by retry unless the producer and contract support replay. Other routes can remain complete or become independently partial.", ["/docs/concepts/realtime-routing.md", "/docs/best-practices/route-sizing.md"]),
    "TRBL-007": ("Session stop returns an incomplete recording or one or more failed stems.", ["A writer rejected frame shape, ordering, or an excessive gap.", "A file operation failed while writing or finalizing one stem.", "Stop reached recording finalization after an earlier component failure."], "Inspect overall recording state, each stem outcome, written-frame counts, discontinuity records, and the stable recording error code.", "Preserve completed stems, correct the failing output path or frame contract, and start a new recording when needed.", "Do not append or retry finalization unless the writer contract permits it. Completed files may be usable while failed stems are partial or absent.", ["/docs/concepts/multistem-recording.md", "/docs/errors/recording.md"]),
    "TRBL-008": ("A connector never reaches readiness or reports a readiness-stage failure.", ["Manifest or resolved configuration validation failed.", "The readiness deadline or threshold is invalid or expires.", "Driver preparation or startup failed.", "The provider boundary reports a typed failure before delivery."], "Read connector stage, error code, retryability, readiness snapshot, probe counts, and configuration context without exposing secrets.", "Correct validation failures or provider configuration, then recreate the connector. Follow `ConnectorRetryability`; do not invent a retry budget.", "Retry is allowed only by the typed retryability and application policy. No delivery acknowledgement should be assumed before readiness.", ["/docs/concepts/connector-workers.md", "/docs/reference/connectors.md"]),
    "TRBL-009": ("Loading a native extension returns a path, library, ABI, descriptor, or registration error.", ["The path is relative, cannot be canonicalized, or is not a regular file.", "The dynamic loader cannot open the library or find the required entry point.", "The ABI version or descriptor is incompatible.", "Registration conflicts and transaction rollback runs."], "Follow the reported stages in order: path validation, library load, entry point, ABI, descriptor, registration, rollback.", "Use a trusted canonical absolute library built for the target and correct the first failing ABI or registration contract.", "Do not repeatedly execute an untrusted or incompatible library. Failed registration is not partial success unless the receipt and registry prove otherwise.", ["/docs/concepts/native-extensions.md", "/docs/reference/native-extensions.md"]),
    "TRBL-010": ("A sidecar fails to start, exchange a message, drain, or stop before its deadline.", ["The child command cannot start or exits early.", "A message kind, framing rule, or byte limit is invalid.", "Startup, request, drain, or abort exceeds its configured deadline.", "The host or child closes while work remains."], "Inspect sidecar state, process error, last accepted message kind, protocol limit, and the exact deadline stage.", "Correct the process or protocol contract. Choose drain only when pending work may complete; choose abort when application policy accepts discarding it.", "Restart is not automatically safe because external work may have occurred before timeout. Treat acknowledgements and output as partial until terminal state confirms them.", ["/docs/concepts/sidecars.md", "/docs/reference/sidecar-protocol.md"]),
    "TRBL-011": ("Opus encoder or decoder construction or conversion returns an error.", ["The sample rate, channels, frame duration, or bitrate is unsupported.", "PCM length does not match the configured frame shape.", "A packet is invalid for the decoder or codec state reports an Opus failure."], "Compare the configured profile with the input frame's `SampleSpec`, samples per channel, packet bounds, and encoder or decoder error variant.", "Recreate codec state with a supported profile or supply a frame or packet that matches the existing state.", "Do not assume the same state can be replayed after a codec failure. The failing packet or frame may not have produced output.", ["/docs/concepts/opus-codec.md", "/docs/reference/codec.md"]),
    "TRBL-012": ("`RunningSession::stop` returns one or more component or finalization failures.", ["A source, route, operator, endpoint, connector, or sidecar fails during stop.", "Recording or trace finalization fails after runtime work completed.", "A worker panics, misses a deadline, or cannot join."], "Inspect the terminal outcome by component and stage. Then inspect recording and trace outcomes separately from the top-level Session stop state.", "Preserve every failure record and any completed outputs. Correct the owning component before starting a new Session.", "A stop failure does not establish a safe restart from the same resources. Frames, files, acknowledgements, and traces can be independently complete or partial.", ["/docs/lifecycle/terminal-outcomes.md", "/docs/errors/session.md"]),
    "TRBL-013": ("A protocol or connector conformance check cannot locate a required vector or private fixture.", ["The repository-owned connector vector has not been copied to the sibling path consumed by the test.", "A private CI fixture is unavailable in the local checkout.", "The materialized fixture revision does not match the repository source."], "Resolve the literal path used by the test, compare it with `scripts/fixtures/connector-v1-vectors.json`, and record the fixture revision before interpreting any assertion result.", "Create the sibling directory, copy the repository-owned vector to its expected path, then rerun the selected check without weakening or skipping the assertion.", "Absence is a prerequisite failure, not a retryable product failure and not passing conformance evidence. No conformance conclusion exists until the fixture runs.", ["/docs/concepts/conformance.md", "/docs/how-to/test-connector-conformance.md"]),
    "TRBL-014": ("The transcription example produces no process-evidence artifact or the artifact does not match the run.", ["The external executable did not start or could not be found.", "Captured input never reached the process step.", "The child output was not persisted or belongs to another source revision."], "Verify child start, exit status, captured output, evidence path, and recorded source revision independently from example compilation.", "Correct the external process prerequisite and rerun the example so it writes a fresh matching artifact.", "Do not treat a compile-only rerun as execution evidence. Capture or process output from an interrupted run can be incomplete.", ["/docs/concepts/transcription-integration.md", "/docs/how-to/run-transcription.md"]),
    "TRBL-015": ("A build with the default native-capture feature fails for the selected target.", ["A target-specific native dependency or development library is unavailable.", "The build script selects a native shim that cannot compile.", "The toolchain or target does not meet package metadata and dependency requirements."], "First run a contracts-only check with default features disabled. Then restore native capture and inspect the failing target dependency or build-script stage.", "Install the target prerequisite or choose contracts-only mode when you do not need native capture.", "Rebuilding after environment correction is safe; it does not prove physical capture qualification. No runtime data exists from a failed build.", ["/docs/concepts/cargo-features.md", "/docs/platform/compatibility.md"]),
    "TRBL-016": ("Mapped timestamps regress, drift grows, or frame lineage reports a discontinuity.", ["Source and Session timestamps were compared without the correct clock-domain mapping.", "The source clock rate changed or accumulated drift.", "Source generation, permission epoch, or discontinuity epoch advanced.", "Timestamp arithmetic is unrepresentable."], "Compare clock-domain IDs, raw source and Session times, mapping generation, drift snapshot, correction, and discontinuity records.", "Rebuild the mapping at a real observation boundary and apply only the controller's evidenced bounds while retaining original lineage.", "Do not retry by rewriting old timestamps. Downstream data around a discontinuity may need an explicit gap or new segment.", ["/docs/concepts/timing-and-clocks.md", "/docs/reference/timing.md"]),
    "TRBL-017": ("Session trace validation returns a structural, identity, ordering, or terminal-record error.", ["A record is out of order or references an inconsistent Session or component ID.", "A required metrics snapshot or terminal record is missing.", "The trace is truncated or belongs to a different run."], "Use the validation error and record index to inspect the first invalid transition, then compare stable IDs and terminal state.", "Fix the recorder or collection workflow and produce a new trace; retain the invalid trace as failure evidence.", "Validation is deterministic for the artifact, so retrying unchanged bytes cannot repair it. The trace may be incomplete and must not be rewritten as observed history.", ["/docs/concepts/session-traces.md", "/docs/reference/lifecycle-evidence.md"]),
    "TRBL-018": ("External PCM buffer acquisition or submission reports saturation, closure, cancellation, or invalid frame shape.", ["All finite input buffers are leased.", "The submitted sample count or format does not match `AudioInputConfig`.", "The input source is closed or its Session was cancelled.", "The downstream route cannot accept the frame."], "Inspect acquisition failures, available slots, outstanding buffers, write error, cancellation state, and downstream route observations.", "Release or submit outstanding buffers, correct frame shape, or create a new input after terminal closure.", "Retry acquisition only after capacity returns. Never replay a submitted frame unless application semantics permit duplication; accepted and rejected frames can leave a partial stream.", ["/docs/concepts/external-pcm.md", "/docs/reference/capture.md"]),
}


BEST_PRACTICES = {
    "BEST-001": ("A guessed route capacity can either saturate under load or reserve more memory than the route needs.", "Measure queue depth, peak depth, saturation, drops, and latency for each route before changing its finite capacity.", "Per-route observations identify the constrained consumer and keep one branch from hiding another branch's behavior.", "Measurement adds instrumentation and a representative workload; a larger capacity can trade memory and latency for fewer immediate rejections.", "Do not reuse one measured capacity for a route with a different producer, consumer, frame size, or loss policy."),
    "BEST-002": ("Allocation, blocking, locks, async scheduling, logging, or application callbacks in the realtime lane can violate its bounded-work contract.", "Keep realtime callbacks limited to the operations accepted by the declared safety contract, and move asynchronous or blocking work to its explicit partition.", "The repository architecture checks and fixed-capacity routes rely on a bounded realtime path.", "Crossing to another partition introduces queue capacity, ownership transfer, and saturation that you must observe.", "The restriction does not apply to an async or blocking worker whose contract explicitly permits that work."),
    "BEST-003": ("Flattening frames into anonymous PCM removes the evidence needed to distinguish sources, generations, routes, and discontinuities.", "Preserve Session, source, stem, stream, route, clock, sequence, generation, and derivation identity through every transformation.", "Lineage lets recordings, operators, and diagnostics attribute output to the exact upstream source state.", "Retaining metadata costs storage and requires transformations to propagate it correctly.", "A final export format may omit internal identity when that loss is explicit and no later diagnostic depends on it."),
    "BEST-004": ("Reducing Session stop to one success flag hides component, recording, sidecar, and trace finalization results.", "Retain and inspect the complete terminal outcome before releasing runtime ownership or reporting success.", "Independent components can complete or fail separately after useful work has already occurred.", "Callers must branch over more structured state and decide how partial outputs are presented.", "A disposable test that asserts one exact failure can inspect only that field when the fixture proves no other output matters."),
    "BEST-005": ("Blindly retrying a connector failure can duplicate provider work or repeat a condition that requires reconfiguration.", "Branch on `ConnectorRetryability`: never retry `Never`, require configuration change for `RetryAfterReconfiguration`, and let application policy decide when to repeat `Retryable` work.", "Retryability is a typed failure property, while observations record attempts. The repository declares no `RetryBudget` type or automatic finite-attempt policy.", "Application code must own timing, attempt limits, idempotency, and provider-specific consequences.", "Do not apply connector retryability to another endpoint or sidecar error unless that boundary exposes the same explicit contract."),
    "BEST-006": ("Loading a native extension executes code and retains callback pointers for the registration lifetime.", "Accept only a host-trusted canonical absolute path to a regular file, then keep the load receipt alive while imported registrations are reachable.", "Path, ABI, descriptor, transaction, and executable-lifetime checks protect the loader's declared boundary.", "Canonicalization and trust policy add deployment work and do not sandbox the loaded code.", "This rule does not apply to a connector that runs exclusively through a separately managed process boundary."),
    "BEST-007": ("Build, VM, conformance, and physical-device evidence answer different questions and can be accidentally promoted into broad support claims.", "Label every platform or provider claim with the command, target, fixture, environment, and evidence class that established it.", "Scoped labels let maintainers distinguish implementation presence from real-device or live-provider qualification.", "Narrow claims are less convenient than one support badge, but they remain auditable and avoid unsupported guarantees.", "You may combine scopes only when the published statement explicitly lists every constituent environment and result."),
}
