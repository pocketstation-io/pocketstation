fn main() {
    // Placeholder binary for future DHAT/custom allocator checks.
    // CI already exercises the example path. Agents should replace this with
    // a real allocation gate before Phase 0 exit.
    println!("allocation check placeholder: implement DHAT gate before Phase 0 exit");
}
