[package]
name = "operator-consumer-example"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[package.metadata.pocketstation]
registry-role = "example"

[dependencies]
pocketstation = { path = "../.." }
whisper-transcribe-example = { path = "../whisper-transcribe" }
